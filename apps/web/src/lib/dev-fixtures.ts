// Données de référence créées par le seed (prisma/seed.ts), partagées avec
// le code applicatif (ex. tests d'intégration). Les comptes utilisateurs ne
// font plus partie de ce seed : ils sont créés via /sign-up.
export const TEST_SITE_NAME = "Saint-Hilaire-du-Touvet";
export const TEST_SCHOOL_NAME = "École de test";
